version: "3.9"

services:
  api:
    build:
      context: ./backend
    ports:
      - "8080:8080"
    restart: unless-stopped

  web:
    build:
      context: ./frontend
      args:
        - VITE_API_BASE=/api
    ports:
      - "3000:80"
    depends_on:
      - api
    restart: unless-stopped
