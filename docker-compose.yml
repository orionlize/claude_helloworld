version: '3.8'

services:
  apihub:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: apihub
    environment:
      - DB_MODE=memory
      - JWT_SECRET=demo-jwt-secret-key
      - SERVER_PORT=8080
    ports:
      - "8080:8080"
    restart: unless-stopped
